A simplified, browser-friendly version inspired by the original [Pika library](https://github.com/hopinc/pika).

**Key Differences:**

- No Node.js dependencies (`crypto`, `os`, `networkInterfaces`)
- Uses `Math.random()` instead of MAC address for node ID generation
